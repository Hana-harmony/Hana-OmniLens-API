services:
  api:
    platform: linux/arm64
    build:
      context: .
    container_name: hana-omni-connect-api
    cpus: "1.40"
    mem_limit: 4g
    env_file:
      - .env.local
    environment:
      JAVA_TOOL_OPTIONS: "-XX:MaxRAMPercentage=70 -XX:+ExitOnOutOfMemoryError"
      SPRING_PROFILES_ACTIVE: local
      SPRING_CONFIG_ADDITIONAL_LOCATION: file:/app/config/application-local.yml
      SPRING_DATASOURCE_URL: jdbc:postgresql://hana-omni-connect-postgres:5432/omni-connect
      SPRING_DATASOURCE_USERNAME: omni-connect
      SPRING_DATASOURCE_PASSWORD: omni-connect-local-password
      SPRING_DATA_REDIS_HOST: hana-omni-connect-redis
      SPRING_DATA_REDIS_PORT: "6379"
      OMNI_CONNECT_AI_HANNAH_BASE_URL: http://hannah-montana-ai:8000
      OMNI_CONNECT_AI_HANNAH_READ_TIMEOUT: 30m
    volumes:
      - ./src/main/resources/application-local.yml:/app/config/application-local.yml:ro
    ports:
      - "127.0.0.1:8080:8080"
    networks:
      - internal
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/actuator/health | grep -q UP"]
      interval: 10s
      timeout: 3s
      retries: 20

  postgres:
    platform: linux/arm64
    image: postgres:17-alpine
    container_name: hana-omni-connect-postgres
    cpus: "0.60"
    mem_limit: 3g
    environment:
      POSTGRES_DB: omni-connect
      POSTGRES_USER: omni-connect
      POSTGRES_PASSWORD: omni-connect-local-password
    ports:
      - "5432:5432"
    volumes:
      - hana-omni-connect-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U omni-connect -d omni-connect"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - internal

  redis:
    platform: linux/arm64
    image: redis:8-alpine
    container_name: hana-omni-connect-redis
    cpus: "0.20"
    mem_limit: 512m
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - hana-omni-connect-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - internal

volumes:
  hana-omni-connect-postgres-data:
  hana-omni-connect-redis-data:

networks:
  internal:
    name: hana-omni-connect-internal
    external: true
